
let {
  // These can also be environment variables
  $env_MY_DNS_PROVIDER_KEY = "a_key"

}  in {

  log_level = "debug"
  iface = "test0"

  ddns = {
    provider = {
      name = "digitalocean"
      key = $env_MY_DNS_PROVIDER_KEY
    }

    domain = "example.com"
    host = "test"
  }
}
